<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScreenPowerPro.Views.CameraOverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Kamera Önizleme">

    <Grid Background="Transparent"
        PointerPressed="OnPointerPressed"
        PointerMoved="OnPointerMoved"
        PointerReleased="OnPointerReleased">

        <!-- Circular or Rounded Camera Container -->
        <Border x:Name="CameraBorder"
            Width="180" Height="180"
            CornerRadius="90"
            Background="#1E1F27"
            BorderBrush="{StaticResource PrimaryAccentBrush}"
            BorderThickness="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <Grid>
                <!-- Webcam visual placeholder or MediaPlayerElement -->
                <MediaPlayerElement x:Name="WebcamPreview"
                    Stretch="UniformToFill"
                    AutoPlay="True"
                    AreTransportControlsEnabled="False"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" />

                <!-- Fallback Icon if camera stream is inactive -->
                <StackPanel x:Name="FallbackPanel"
                    HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                    <FontIcon Glyph="&#xE714;" FontSize="36" Foreground="{StaticResource PrimaryAccentBrush}" />
                    <TextBlock Text="Kamera Aktif" FontSize="11" FontWeight="SemiBold"
                        Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Floating close & shape toggle buttons on hover -->
                <StackPanel Orientation="Horizontal" Spacing="4"
                    HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8">
                    <Button Click="OnToggleShapeClicked"
                        Background="#99000000" BorderThickness="0" CornerRadius="12"
                        Width="24" Height="24" Padding="0" ToolTipService.ToolTip="Şekil Değiştir">
                        <FontIcon Glyph="&#xE790;" FontSize="11" Foreground="White"/>
                    </Button>
                    <Button Click="OnCloseClicked"
                        Background="#99000000" BorderThickness="0" CornerRadius="12"
                        Width="24" Height="24" Padding="0" ToolTipService.ToolTip="Kapat">
                        <FontIcon Glyph="&#xE711;" FontSize="10" Foreground="White"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
