<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ScreenPowerPro.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBgBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="800" Margin="32,24,32,48" Spacing="24">

            <!-- Header -->
            <StackPanel Spacing="4">
                <TextBlock Text="Ayarlar" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                <TextBlock Text="Uygulama tercihleri, çekirdek seviyesi Win32 izinleri ve kayıt parametreleri" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
            </StackPanel>

            <!-- Success InfoBar -->
            <InfoBar x:Name="SaveSuccessInfoBar" IsOpen="False" Severity="Success" Title="Başarılı" Message="Ayarlar kaydedildi." />

            <!-- Core / Win32 Level Features -->
            <Border Background="{StaticResource SurfaceContainerBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="ÇEKİRDEK SEVİYESİ / WIN32 SİSTEM İZİNLERİ" FontSize="11" FontWeight="Bold" Foreground="{StaticResource PrimaryAccentBrush}" CharacterSpacing="50" />

                    <!-- Exclude App Windows -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Kontrol Pencerelerini Kayıttan Gizle" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Text="Kayıt çubuğu ekranda görünse bile nihai video çıktısında asla yer almaz (WDA_EXCLUDEFROMCAPTURE)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.ExcludeAppFromRecording, Mode=TwoWay}" OnContent="" OffContent="" MinWidth="0" />
                    </Grid>

                    <MenuFlyoutSeparator Background="{StaticResource BorderBrush}" />

                    <!-- Hide Mouse Cursor -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Fare İmlecini Videodan Gizle" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Text="Video kaydına ana fare imleci yansımaz, sadece tıklama efektleri simüle edilir" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.HideMouseCursor, Mode=TwoWay}" OnContent="" OffContent="" MinWidth="0" />
                    </Grid>

                    <MenuFlyoutSeparator Background="{StaticResource BorderBrush}" />

                    <!-- Hide Desktop Icons -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Kayıt Sırasında Masaüstü Simgelerini Gizle" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Text="Kayıt başladığında masaüstü simgeleri gizlenir, bittiğinde otomatik geri getirilir" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.HideDesktopIcons, Mode=TwoWay}" OnContent="" OffContent="" MinWidth="0" />
                    </Grid>

                    <MenuFlyoutSeparator Background="{StaticResource BorderBrush}" />

                    <!-- Hide Taskbar -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Kayıt Sırasında Windows Görev Çubuğunu Gizle" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Text="Tam ekran kayıtlarda temiz bir görünüm için görev çubuğunu geçici gizler" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.HideTaskbar, Mode=TwoWay}" OnContent="" OffContent="" MinWidth="0" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Video & Audio Quality -->
            <Border Background="{StaticResource SurfaceContainerBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="KAYIT VE ÇIKTI KALİTESİ" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" CharacterSpacing="50" />

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- FPS -->
                        <StackPanel Spacing="6">
                            <TextBlock Text="Kare Hızı (FPS)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                            <ComboBox SelectedValue="{x:Bind ViewModel.Fps, Mode=TwoWay}" HorizontalAlignment="Stretch" CornerRadius="8" Background="{StaticResource SurfaceHighBrush}">
                                <x:Int32>60</x:Int32>
                                <x:Int32>30</x:Int32>
                            </ComboBox>
                        </StackPanel>

                        <!-- Resolution -->
                        <StackPanel Grid.Column="1" Spacing="6">
                            <TextBlock Text="Varsayılan Çözünürlük" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                            <ComboBox SelectedValue="{x:Bind ViewModel.Resolution, Mode=TwoWay}" SelectedValuePath="Content" HorizontalAlignment="Stretch" CornerRadius="8" Background="{StaticResource SurfaceHighBrush}">
                                <ComboBoxItem Content="1080p" />
                                <ComboBoxItem Content="4K" />
                                <ComboBoxItem Content="720p" />
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Folders -->
            <Border Background="{StaticResource SurfaceContainerBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="DOSYA VE KAYIT KONUMLARI" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" CharacterSpacing="50" />

                    <StackPanel Spacing="6">
                        <TextBlock Text="Proje Kayıt Klasörü" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBox Text="{x:Bind ViewModel.ProjectSaveLocation, Mode=TwoWay}" CornerRadius="8" Background="{StaticResource SurfaceHighBrush}" BorderBrush="{StaticResource BorderBrush}" />
                    </StackPanel>

                    <StackPanel Spacing="6">
                        <TextBlock Text="Video Export Klasörü" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBox Text="{x:Bind ViewModel.ExportLocation, Mode=TwoWay}" CornerRadius="8" Background="{StaticResource SurfaceHighBrush}" BorderBrush="{StaticResource BorderBrush}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Save Button -->
            <Button Click="OnSaveClicked" Background="{StaticResource PrimaryButtonGradient}" BorderThickness="0" CornerRadius="10" Padding="24,12" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74E;" FontSize="14" Foreground="White" />
                    <TextBlock Text="Değişiklikleri Kaydet" FontWeight="Bold" FontSize="13" Foreground="White" />
                </StackPanel>
            </Button>

        </StackPanel>
    </ScrollViewer>
</Page>
